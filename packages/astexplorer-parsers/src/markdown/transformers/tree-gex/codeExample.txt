import { walkReplace, transform, any } from "tree-gex";

// Increase all heading depths by 1 (# -> ##, ## -> ###).
// `ast` is the remark AST. The return value is stringified back to Markdown.
export default walkReplace(ast, {
  type: "heading",
  depth: transform(any(), (d) => d + 1),
});
