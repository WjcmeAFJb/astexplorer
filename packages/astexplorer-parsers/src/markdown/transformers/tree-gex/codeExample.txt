import * as w from "tree-gex";

// Increase all heading depths by 1 (# -> ##, ## -> ###).
// The return value is stringified back to Markdown.
export default w.walkReplace(ast, {
  type: "heading",
  depth: w.transform(w.any(), (d) => d + 1),
});
