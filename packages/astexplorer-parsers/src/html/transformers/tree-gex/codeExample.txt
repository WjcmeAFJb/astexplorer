import { accumWalkMatch, string, group } from "tree-gex";

// Find all HTML elements and capture their tag names.
// `ast` is the parsed HTML tree.
export default accumWalkMatch(ast, {
  tag: group(string(), "tagName"),
});
