import * as w from "tree-gex";

// Replace all 'a' characters with 'b' in the regex.
// Result is printed back to a regex string.
export default w.walkReplace(ast, {
  type: "Char",
  kind: "simple",
  value: w.transform(w.string(), (v) => (v === "a" ? "b" : v)),
});
