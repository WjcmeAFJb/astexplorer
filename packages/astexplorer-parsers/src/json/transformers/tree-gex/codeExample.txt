import * as w from "tree-gex";

// Find all property nodes and capture their keys.
export default w.accumWalkMatch(ast, {
  type: "Property",
  key: {
    type: "Identifier",
    value: w.group(w.string(), "key"),
  },
});
