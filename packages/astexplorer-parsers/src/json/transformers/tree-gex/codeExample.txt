import { accumWalkMatch, string, group } from "tree-gex";

// Find all property nodes and capture their keys.
// `ast` is the JSON AST parsed by json-to-ast.
export default accumWalkMatch(ast, {
  type: "Property",
  key: {
    type: "Identifier",
    value: group(string(), "key"),
  },
});
