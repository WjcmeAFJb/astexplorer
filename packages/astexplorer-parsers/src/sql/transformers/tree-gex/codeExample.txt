import { accumWalkMatch, string, group } from "tree-gex";

// Find all nodes with a "type" property and capture their type names.
// `ast` is the parsed SQL AST.
export default accumWalkMatch(ast, {
  type: group(string(), "nodeType"),
});
