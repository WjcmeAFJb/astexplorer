import { accumWalkMatch, any, string, group } from "tree-gex";

// Find all mapping nodes and capture their values.
// `ast` is the YAML AST parsed by yaml-ast-parser.
export default accumWalkMatch(ast, {
  kind: group(any(), "nodeKind"),
  value: group(string(), "nodeValue"),
});
