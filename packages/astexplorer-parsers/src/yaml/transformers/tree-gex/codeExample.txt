import * as w from "tree-gex";

// Find all mapping nodes and capture their values.
export default w.accumWalkMatch(ast, {
  kind: w.group(w.any(), "nodeKind"),
  value: w.group(w.string(), "nodeValue"),
});
