import * as w from "tree-gex";

// Find all CSS declarations and capture their property names and values.
export default w.accumWalkMatch(ast, {
  type: "decl",
  prop: w.group(w.string(), "property"),
  value: w.group(w.string(), "value"),
});
