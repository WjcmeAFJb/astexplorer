import { accumWalkMatch, string, group } from "tree-gex";

// Find all CSS declarations and capture their property names and values.
// `ast` is the parsed PostCSS AST of the CSS in the left pane.
export default accumWalkMatch(ast, {
  type: "decl",
  prop: group(string(), "property"),
  value: group(string(), "value"),
});
