import { accumWalkMatch, string, group } from "tree-gex";

// Find all field definitions and capture their names.
// `ast` is the GraphQL AST parsed by graphql-js.
export default accumWalkMatch(ast, {
  kind: "FieldDefinition",
  name: {
    kind: "Name",
    value: group(string(), "fieldName"),
  },
});
