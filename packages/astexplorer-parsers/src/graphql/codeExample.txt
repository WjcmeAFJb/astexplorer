# Paste or drop a GraphQL schema or query here
# and explore the syntax tree.

type Query {
  cartItem(cartId: ID! @isId, itemId: ID!): CartItem
    @requiresPermission(permission: "cart.view")
}

type Mutation {
  deleteItem(id: ID! @isId): Boolean
    @requiresPermission(permission: "cart.modify")
}
